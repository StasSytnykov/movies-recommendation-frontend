const BoxStyles = { display: "flex", justifyContent: "space-between" };

const BoxStylesInBox = { display: "flex" };

const ByttonStyles = { ml: "20px", width: "150px" };

export { BoxStyles, ByttonStyles, BoxStylesInBox };
