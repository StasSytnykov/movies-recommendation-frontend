import { red } from "@mui/material/colors";

const PaperStyles = {
  p: "2px 4px",
  display: "flex",
  alignItems: "center",
  width: "100%",
};

const InputBaseStyle = { ml: 1, flex: 1 };

const TypographyStyle = { color: red[500] };

export { PaperStyles, InputBaseStyle, TypographyStyle };
