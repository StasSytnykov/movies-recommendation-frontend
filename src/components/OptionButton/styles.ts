import { grey } from "@mui/material/colors";

const BoxStyles = { position: "absolute", left: 5, top: 5, zIndex: 100 };

const IconButtonStyles = {
  backgroundColor: grey[100],
  opacity: 0.8,
  height: 30,
  width: 30,
};

const MenuStyles = { position: "absolute" };

export { BoxStyles, IconButtonStyles, MenuStyles };
