const CardStyles = { display: "flex", position: "relative" };

const CardMediaStyles = { width: 100 };

const BoxStyles = { display: "flex", flexDirection: "column", width: "100%" };

const CardContentStyles = { flex: "1 0 auto", padding: 1 };

export { CardStyles, CardMediaStyles, BoxStyles, CardContentStyles };
